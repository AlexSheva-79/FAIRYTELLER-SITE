# Исходники PNG (рабочие файлы для конвертации в WebP) — не используются сайтом
/[0-9].png
/[0-9][0-9].png
/source/
/cyberpunk/
/fantasyart/
/streetart/

# Референсы макетов — использовались при вёрстке, на сайте не нужны
/превью*.png
/баннер превью.png

# Исходники видео для баннера (тяжёлые, необработанные) — на сайте используются
# сжатые версии из assets/video/
/video/
